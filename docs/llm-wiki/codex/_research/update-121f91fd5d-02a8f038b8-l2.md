# L2 / L3 — Codex Wiki `121f91fd5d` → `02a8f038b8`

对照源码 `codex/` @ `02a8f038b8`。独立 subagent 不采信 filler / `status:verified`。

## 覆盖

| 批次 | 节点数 | 结果 |
|---|---:|---|
| broken | 5 | verified；glossary `ServerNotification` 83→84、`ResponseItem` 17→18；若干错位 `[E]` |
| heavy | 5 | verified；驳回 PowerShell AST 已退出 production、`decide_approval` 先问 extensions、schema `read`/`write` 即协议字段 |
| catalogs | 7 | verified；亲手重数 147/142/29/83/167/84/11/29；大量 crate/feature 行号重落 |
| rpc-slash | 7 | verified；attachment 三方法稳定、`memory/status` experimental、`/voice` 由 `voice_command_enabled` 过滤、`SandboxReadRoot` 已删；驳回「`/fork` 不再发 `ForkCurrentSession`」 |
| spine-tui-auth | 8 | verified；folder consent 在 destination 解析后；`user-verification` 只做本地签名；驳回 workload-identity「context marker 也选中」 |
| core-drift | 8 | verified；无新 model-visible core tool；`ApiKeyModelDiscovery` 默认关；CompactTask 只有 token-budget / remote v2 / local |
| leftover-high-value | 8 | verified；机械 rebase 留下的 turn-metadata / network-proxy / Python `ExternalMessage` cite 已改 |

## L3 回源后仍改的项

1. `subsys.core.approval-guardian-v2`：删除「guardian 还可挂只读 `history.*`」。`spec_plan.rs:156` / `:1029` 对 `is_basic_session_source` 整段 `return`，测试只断言 `exec_command` / `write_stdin` / `view_image`。
2. `rpc.notifications-system`：50+33=83 改为 51+33=**84**。
3. `subsys.mcp.server`：`rmcp-client` 行号 98→97；members L3–L147→L3–L149。

## 未做

其余 ~137 个仅 SHA bump / exact `[E]` rebase 的 C-DRIFT / D-CLEAN 节点没有逐页 L2。127 条 contextual `[E]` rebase 因 LOW_CONFIDENCE 未写。
