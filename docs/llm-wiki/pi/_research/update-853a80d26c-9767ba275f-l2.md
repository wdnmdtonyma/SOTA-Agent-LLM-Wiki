# L2 verification — 853a80d26c → 9767ba275f

## Scope facts

- official default branch: `origin/main`
- base: `853a80d26c90a14c1886f0ebb8ffaae133ca2185`
- target: `9767ba275f3e9a5ee0f5c5342249b629ab1b2282`
- upstream span: 463 commits; 757 files; +98,743 / -26,740
- result: 200 verified nodes; +2 / -0; 0 planned
- upstream tags: v0.85.0 / v0.85.1（另含一小段 Unreleased）

## Independent verifier matrix

| Area | Result | Notes |
|---|---|---|
| catalogs（11 groups + tools / image / session-events） | PASS after L3 | env 103、keybindings 90、image 52、session-events 23 unique types、extension events 36、TUI components 16 |
| spine / process-lifecycle / extension-lifecycle | PASS after L3 | Node 入口是 `setupCli()` + `main(...)`；Bun 静态 import；`builtInExtensions` 在 offline/auth 前展开 |
| tools + constrained sampling | PASS after L3 | write 成功文案 `Successfully wrote to ${path}`；read/bash/powershell/edit/write 默认 json_schema strict prefer |
| coding-agent system-prompt / theme / session | PASS after L3 | skills 门控是 read **或** bash；theme 用户文档写 53 required tokens |
| AI / Google thinking / model-discovery | PASS after L3 | `level === "off"` 先返回 `"high"`；xAI 内置排除 grok-build-0.1 |
| D-CLEAN（38 页） | PASS | 未发现把已删符号写成现行 API |

L3 修了确认过的 error（catalog 漏项与跨页假话），以及若干高价值 DRIFT（错误函数/行号）。大量 rebase nit 未系统改：`[E:]` 有时落到邻近符号或注释，句子仍对。

## Catalog audit

对照 `9767ba275f` 源码重数：

- runtime providers: 40（`builtinProviders()`）
- static model buckets: 39
- image models: 52（`IMAGE_MODELS.openrouter`；补 `microsoft/mai-image-2.6` / `mai-image-2.6-flash`）
- config keys 84、slash 23、RPC 33、CLI 63、extension events **36**（含多行 overload `session_before_switch` / `session_before_compact` / `before_provider_request`）
- keybindings **90**（47 tui + 43 app，含 `app.thinking.save`）
- env **103**（补 `PI_TUI_WRITE_LOG`、`PI_TUI_DEBUG`；另有已存在的 `PI_TUI_DEBUG_REDRAW` / `PI_TUI_ESC_TIMEOUT`）
- interactive components 43、TUI components **16**（含 `MouseRegion`）
- `AgentSessionEvent` unique types **23**（补 `agent_settled` / `entry_appended` / `summarization_retry_*` / `bash_execution_update`）
- built-in tools: 8；`createCodingToolDefinitions` / `createReadOnlyToolDefinitions` 仍不含 `powershell`

`_staging/UPDATE-INSTRUCTIONS.md` item 14 曾误写「events=33，不要沿用 36」；已改回源码 36。

## Confirmed errors that L3 fixed

- `ref.coding-agent.default-keybindings`：89→90，补 `app.thinking.save`；`/thinking` persist 走该 action
- `ref.coding-agent.env-vars`：101→103，补 `PI_TUI_WRITE_LOG`（`terminal.ts:150`）、`PI_TUI_DEBUG`（`tui-main-screen.ts:569`）
- `ref.coding-agent.session-events`：实例表补齐 6 个 session-owned type
- `ref.ai.image-models`：50→52
- `ref.error-codes`：`FileSystem.cleanup` 是 `Promise<void>`（`harness/types.ts:314`），不是一律 `Result`
- `ref.glossary`：Radius 不是已删除术语
- `surface.tools.write`：成功文案不再带 byte count；render 在 `core/tools/renderers/write.ts`
- `spine.process-lifecycle`：`cli/setup.ts` + 静态 Bun import
- `subsys.coding-agent.system-prompt`：关 read 留 bash 仍注入 skills
- `subsys.ai.google-generative-ai`：`resolveGoogleThinkingLevel` 的 `off`→`high`

## Remaining nits

- rebase 常把 `[E:]` 落到邻近符号（例如 image-models 个别 cost 行号串到下一模型；client/server/harness 握手行号）。句子仍对，高价值页可再改准。
- theme：**用户文档**写 53 required tokens；`ThemeJsonSchema.colors` 实际是 51 个非 Optional + 5 个 Optional（`scrollbarTrack` / `scrollbarThumb` / `searchMatchBg` / `searchMatchText` / `thinkingMax`）。wiki 已把 53 归因到 `themes.md`，没有把 schema 说成 53。
- 历史 `_staging/uncertainty-*.md` 仍可能提到已删符号（如旧 `InMemorySessionRepo`）；reconcile 会原样并进 `reference/uncertainty.md`。

## Validation

Wiki validation is serial after fillers and L3: reconcile, lint, reconcile again, lint again. Final: 0 error / 0 warning · 200 nodes.

`index.groups` 已手改：env 103、keybindings 90、TUI components 16。reconcile 不同步 `groups[].instance_count`。

Upstream runtime tests were not executed: detached Pi checkout has no `node_modules`.
