# L2 verification — c1019d9202 → 0e633790c5

## Scope facts

- official default branch: `origin/main`
- base: `c1019d9202b648143d123b7e6fb76543a6b82de6`
- target: `0e633790c5a007f6d4bf35ba67ced457287c25ac`
- upstream span: 1 commit; 2 files; +3 / -1
- node impact: A-BROKEN 0 / B-HEAVY 0 / C-DRIFT 2 (1 direct + 1 transitive) / D-CLEAN 200
- result: 202 verified nodes; 0 added; 0 retired; 0 planned

## Facts to falsify

1. The pattern accepts one or more concatenated complete `?997;1n/2n` reports and rejects bytes outside that anchored batch.[E: packages/tui/src/terminal-colors.ts:29]
2. A repeated capture yields the last report, so `2,1,1` returns `dark` and `1,2,2` returns `light`.[E: packages/tui/src/terminal-colors.ts:68] [E: packages/tui/src/terminal-colors.ts:72] [E: packages/tui/test/terminal-colors.test.ts:118] [E: packages/tui/test/terminal-colors.test.ts:119]
3. If a `Terminal` adapter delivers a complete concatenated batch in one callback, `TuiBase.handleTerminalInput()` consumes it before normal input listeners and focused-component dispatch, and invokes each scheme listener once with the parser's final scheme.[E: packages/tui/src/tui.ts:792] [E: packages/tui/src/tui.ts:796] [E: packages/tui/src/tui.ts:800] [E: packages/tui/src/tui.ts:858] [E: packages/tui/src/tui.ts:863] [E: packages/tui/src/tui.ts:892] [E: packages/tui/src/tui.ts:893] [E: packages/tui/src/tui.ts:898] [E: packages/tui/src/tui.ts:899] [E: packages/tui/src/tui.ts:901]
4. The commit does not change OSC 11 parsing, input framing, key parsing, or catalog membership.

## Independent verifier result

The first focused read-only L2 returned **FAIL** and found two counterexamples:

- `subsystems/tui/key-pipeline.md` still named the consumer `TUI.handleInput` and cited stale lines; the target code binds `TuiBase.start()` to `this.handleTerminalInput(data)` at `tui.ts:680-686`.[E: packages/tui/src/tui.ts:680] [E: packages/tui/src/tui.ts:683] [E: packages/tui/src/tui.ts:684] [E: packages/tui/src/tui.ts:685]
- The draft treated a raw stdin batch as one unconditional consumer callback. In the default `ProcessTerminal` path, `StdinBuffer` extracts every complete CSI sequence and `setupStdinBuffer()` forwards each emitted sequence separately.[E: packages/tui/src/stdin-buffer.ts:192] [E: packages/tui/src/stdin-buffer.ts:207] [E: packages/tui/src/stdin-buffer.ts:231] [E: packages/tui/src/stdin-buffer.ts:254] [E: packages/tui/src/terminal.ts:181] [E: packages/tui/src/terminal.ts:191]

The Wiki was corrected to distinguish default `ProcessTerminal` framing from the parser/consumer compatibility case where an adapter supplies a complete concatenated batch in one callback. It also now records the actual gate order: color reports before input listeners, cell-size responses after listeners, then debug and focused-component dispatch.[E: packages/tui/src/tui.ts:792] [E: packages/tui/src/tui.ts:796] [E: packages/tui/src/tui.ts:800] [E: packages/tui/src/tui.ts:818] [E: packages/tui/src/tui.ts:823] [E: packages/tui/src/tui.ts:858]

The same independent verifier then re-read the remediated Wiki and returned **PASS**. It confirmed the corrected method name and binding, gate order, adapter-vs-default-framing distinction, listener/focused-dispatch anchors, regex/repeated-capture behavior, invalid boundaries, source/Sources/index synchronization, and 202-node target SHA consistency. The verifier remained read-only.

## Validation

The focused 0e633 change passed its serial validation, but the required pre-commit fetch then observed `origin/main` at `305c014dcccfe97ebd3f4057ac16c436f1e2c71e`; no 0e633-only commit was created. The expanded frozen-target validation is recorded in `_UPDATE-SCOPE.md` and `_research/update-0e633790c5-305c014dcc-l2.md`.
