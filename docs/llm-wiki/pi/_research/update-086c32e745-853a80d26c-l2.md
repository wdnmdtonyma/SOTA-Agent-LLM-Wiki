# L2 verification — 086c32e745 → 853a80d26c

## Scope facts

- official default branch: `origin/main`
- base: `086c32e74530564922d011ade23ff582c9d63116`
- target: `853a80d26c90a14c1886f0ebb8ffaae133ca2185`
- upstream span: 141 commits; 268 files; +10,448 / -2,519
- result: 198 verified nodes; +1 / -0; 0 planned
- upstream tags: v0.84.3 / v0.84.4

## Independent verifier matrix

| Area | Result | Notes |
|---|---|---|
| catalogs (11 groups + tools) | 11/11 PASS | slash 23、RPC 33、events 36、config 84、keybindings 89、env 98、CLI 63、interactive 43、providers 40、model buckets 39、tools 8 |
| powershell + agent-loop + hooks | PASS after L3 | `prepareNextTurn` 只在还会再开一轮 assistant turn 之后运行；powershell 不在 coding/read-only preset |
| /share + settings persist | PASS after L3 | Radius 失败不回退 gist；`/model` `/thinking` Enter=session-only，Ctrl+S persist |
| bash schema / spawn / executionMode | PASS after L3 | 修正 `Shell command to execute` 与 spawn/executionMode 行号 |

L3 修了 1 条 FAIL（bash schema description）和若干 DRIFT（注释/函数头锚点）。

## Catalog audit

- runtime providers: 40
- static model buckets: 39（完整模型值在 gitignored generated JSON）
- Qwen Individual allowlist: 8（含 `deepseek-v4-pro-0813`；排除 `qwen3.8-max-preview`）
- config keys 84、keybindings 89、env 98、slash 23、RPC 33、CLI 63、extension events 36、interactive components 43
- built-in tools: 8；`createCodingToolDefinitions` / `createReadOnlyToolDefinitions` 仍不含 `powershell`

## Validation

Wiki validation is serial after fillers and L3: reconcile, lint, reconcile again, lint again. Final: 0 error / 0 warning.

Upstream runtime tests were not executed: detached Pi checkout has no `node_modules`.
